
alias fishbs="nano ~/.config/fish/config.fish"

alias bs="nano ~/.bashrc"
alias sw="sudo swapoff -a; sudo swapon -a"

export PATH="${PATH}:~/.local/bin"
export PYTHONPATH="${PYTHONPATH}:~/.local/bin"
